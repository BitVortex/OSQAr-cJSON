# Auto-generated by OSQAr — gsn2x safety case specification
# Render with: gsn2x gsn_safety_case.yaml
# Install from: https://github.com/jonasthewolf/gsn2x/releases

C1:
  text: '**AoU-1 (Integration Context):** cJSON is integrated as a stateless library in an ASIL D ECU context. All state is
    caller-owned (cJSON* pointers). The integrator provides a qualified memory allocator via cJSON_InitHooks.'
C2:
  text: '**AoU-4 (Error Handling):** The integrator shall check all cJSON return values for error indicators (NULL from parse/create
    functions, cJSON_False from boolean returns). Unchecked error paths are the integrator''s responsibility.'
C3:
  text: '**AoU-2 (Input Constraints):** JSON inputs processed through cJSON shall not exceed CJSON_NESTING_LIMIT (1000) depth.
    Maximum single string length is bounded by available memory. Inputs originate from a trusted or integrity-checked source
    (validated at system level before reaching cJSON).'
C4:
  text: '**AoU-3 (Threading Model):** cJSON is not thread-safe and shall be used from a single thread of control or externally
    synchronized by the integrator.'
C5:
  text: '**AoU-5 (Toolchain):** The integrator shall reproduce the qualification build with the audited toolchain version
    and compiler flags. Binary equivalence or semantic equivalence shall be demonstrated.'
C6:
  text: The cJSON SEooC qualification baseline is v1.7.19 (identified by git tag and SHA256 of the source archive). All artifacts
    (requirements, architecture, verification reports, builds) are version-controlled under the OSQAr project.
C7:
  text: All deviations from requirements, static analysis false positives, and coverage gaps shall be documented as issue
    records with safety justifications, reviewer sign-off, and traceability to affected requirements.
C8:
  text: 'The cJSON SEooC qualification lifecycle follows ISO 26262-10 Clause 6 (SEooC development). The lifecycle encompasses
    four phases: (1) Assumption of Use definition, (2) Safety Requirements derivation from assumptions, (3) Verification and
    Validation against requirements, (4) Integration validation in target context.'
A1:
  text: All integer arithmetic in parsing and printing shall be free of signed overflow, wraparound, and truncation that could
    produce incorrect JSON values.
A2:
  text: cJSON shall not contain unreachable dead code, unused variables, or logical contradictions that could indicate dormant
    defects.
A3:
  text: All memory allocations shall be checked for failure. Any failed allocation shall result in a deterministic error return
    (NULL) without undefined behavior.
A4:
  text: The library shall not invoke undefined behavior for any input, including malformed JSON, integer overflow in depth/buffer-size
    parameters, and NULL pointer arguments.
A5:
  text: The cJSON API functions shall validate their input pointer parameters before dereference. NULL pointers passed as
    cJSON* arguments shall produce deterministic error returns.
A6:
  text: The cJSON parser shall accept all valid JSON texts as defined by RFC 8259 and reject all malformed inputs with a deterministic
    error state.
A7:
  text: The cJSON printer shall produce valid JSON text conforming to RFC 8259 for any valid in-memory cJSON object graph.
A8:
  text: The cJSON build system shall be reproducible. A build from source on the qualified toolchain version shall produce
    bit-identical or semantically equivalent binaries.
A9:
  text: cJSON shall operate with bounded stack usage. Recursive descent parsing shall be depth-limited to prevent stack overflow.
A10:
  text: String handling in cJSON shall be safe against buffer overruns. All string operations shall respect buffer boundaries
    and produce NUL-terminated results.
A11:
  text: All safety requirements shall be traceable to verification activities, and all verification activities shall produce
    documentation artifacts suitable for ISO 26262-8:2025 Clause 9 (Verification) audit.
A12:
  text: cJSON_Utils functions (JSON Patch RFC 6902, JSON Pointer RFC 6901) shall produce correct outputs conforming to their
    respective RFCs and reject malformed patch/pointer inputs.
GCJSON_SAFE:
  text: cJSON v1.7.19 is sufficiently safe to be integrated as a Safety Element out of Context (SEooC) into an ASIL D automotive
    system, subject to the Assumptions of Use defined in LM_AOU_CONTEXT through LM_AOU_TOOLCHAIN.
  inContextOf:
  - C1
  - C2
  - C3
  - C4
  - C5
  - C9
  supportedBy:
  - S1
S1:
  text: Argument by 4 safety goals
  supportedBy:
  - GPARSE_SAFETY
  - GVERIFICATION_COMPLETE
  - GNO_UB
  - GMEMORY_SAFETY
GPARSE_SAFETY:
  text: cJSON correctly parses all valid JSON (RFC 8259) and deterministically rejects all malformed inputs.
  supportedBy:
  - Sn1
  - Sn2
  inContextOf:
  - A6
Sn1:
  text: Fuzz-test the cJSON parser with AFL++ or libFuzzer for a minimum of 24 CPU-hours. All crashes, hangs, and assertion
    failures shall be triaged and resolved.
Sn2:
  text: Execute the full cJSON test suite (unit tests for parser, printer, utilities, RFC conformance) and confirm all tests
    pass with zero failures.
GVERIFICATION_COMPLETE:
  text: All safety requirements are verified with adequate coverage and documented evidence suitable for ISO 26262-8 audit.
  supportedBy:
  - Sn3
  - Sn4
  inContextOf:
  - A11
Sn3:
  text: Measure cyclomatic complexity of all cJSON functions. Functions exceeding McCabe 15 shall be reviewed and justifications
    documented.
Sn4:
  text: 'Measure statement and branch coverage of the test suite on the cJSON source. Target: ≥ 90% statement coverage, ≥
    80% branch coverage. Document uncovered branches with safety justifications.'
GNO_UB:
  text: cJSON does not invoke undefined behavior for any input, including integer overflow, NULL dereference, and malformed
    data.
  supportedBy:
  - Sn5
  - Sn6
  - Sn7
  inContextOf:
  - A4
Sn5:
  text: Verify NULL-pointer safety by constructing test cases that pass NULL to every public API function and confirming deterministic
    error returns.
Sn6:
  text: Audit integer operations in parse_number, print_number, and buffer-size computation for signed overflow and wraparound
    vulnerabilities. Verify with UBSan integer checks.
Sn7:
  text: Run AddressSanitizer (ASan) and UndefinedBehaviorSanitizer (UBSan) instrumented test builds to detect runtime memory
    errors and undefined behavior.
GMEMORY_SAFETY:
  text: cJSON is free from memory errors (heap corruption, use-after-free, double-free, buffer overrun, uninitialized reads).
  supportedBy:
  - Sn8
  - Sn9
  inContextOf:
  - A3
Sn8:
  text: Run AddressSanitizer (ASan) and UndefinedBehaviorSanitizer (UBSan) instrumented test builds to detect runtime memory
    errors and undefined behavior.
Sn9:
  text: Run Valgrind/Memcheck on the test suite to verify absence of memory leaks, use-after-free, uninitialized reads, and
    invalid memory accesses.
C9:
  text: '**SEooC Assumptions of Use (ISO 26262-10:2025 §6.4.3):** (1) cJSON is integrated as a stateless library with caller-owned
    state and a qualified memory allocator provided by the integrator via cJSON_InitHooks. (2) JSON inputs do not exceed CJSON_NESTING_LIMIT
    (1000) depth; max string length is bounded by available memory; inputs originate from a trusted or integrity-checked source.
    (3) Single-threaded use or external synchronization by integrator. (4) All cJSON return values are checked by integrator;
    NULL/cJSON_False indicate errors. (5) Qualification build is reproduced with audited toolchain and compiler flags. See
    :need:`LM_AOU_CONTEXT`, :need:`LM_AOU_INPUTS`, :need:`LM_AOU_THREADING`, :need:`LM_AOU_ERROR`, :need:`LM_AOU_TOOLCHAIN`.'
