osqar==0.8.1
# All Sphinx/docs dependencies (sphinx-needs, sphinxcontrib-plantuml, furo,
# sphinx-test-reports, pyyaml) are pulled transitively by OSQAr.
# Only install lizard separately — it's an optional OSQAr evidence tool.
#   pip install lizard
